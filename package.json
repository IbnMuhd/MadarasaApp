{
  "homepage": "https://IbnMuhd.github.io/MadarasaApp",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
    // ... other scripts like "start", "build", etc.
  }
}

{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "deploy": "gh-pages -d build",
    "build": "react-scripts build"
  },
  "dependencies": {
    "lucide-react": "^0.545.0",
    "react-dom": "^19.2.0",
    "react-scripts": "^0.0.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
}